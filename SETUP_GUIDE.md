# Supabase Setup Guide

おまかせ（自動設定）ができないため、以下の手順で「箱（データベース）」だけ作成をお願いします。
無料で作成でき、チャットで送信するための「URL」と「Key」が手に入ります。

## 手順 (所要時間 3分)

1.  **Supabaseにアクセス**
    Access: [https://supabase.com/dashboard](https://supabase.com/dashboard)
    "New Project" をクリックして新規作成します。

2.  **プロジェクト設定**
    - **Name**: `Keiba2026` (なんでもOK)
    - **Database Password**: (忘れないようにメモしてください)
    - **Region**: `Tokyo (North East Asia)` を選択
    - "Create new project" をクリック（作成に1〜2分かかります）

3.  **テーブル作成 (SQL実行)**
    - 左メニューの **SQL Editor** (アイコン: 📄\_ ) をクリック
    - "New Query" をクリック
    - **以下のSQLコードをコピー＆ペーストして、右下の「Run」ボタンを押す**

    ```sql
    -- ここからコピー
    create table users (
      id uuid default gen_random_uuid() primary key,
      name text not null,
      jockey text not null,
      pin text not null
    );

    create table bets (
      id uuid default gen_random_uuid() primary key,
      user_id uuid references users(id) not null,
      race_id text not null,
      investment int default 0,
      return_amount int default 0,
      created_at timestamp with time zone default now()
    );

    alter publication supabase_realtime add table bets;

    insert into users (name, jockey, pin) values
    ('ウグイスバレー', '原田', '0000'),
    ('ニンゲンビレッジ', '矢橋', '0000'),
    ('チェンジドライバ', '岡本', '0000'),
    ('エセドバイオー', '安井', '0000'),
    ('サイレントイナバ', '稲葉', '0000'),
    ('ブームオレタ', '櫛部', '0000'),
    ('ツチサカ', '土坂', '0000'),
    ('イトウ', '伊藤', '0000'),
    ('ハンケン', '富田', '0000'),
    ('アサミハズバンド', '大橋', '0000'),
    ('オオクボハグルマ', '大久保', '0000'),
    ('キンパチティーチャ', '佐久間', '0000');
    -- ここまで
    ```

4.  **キーの取得**
    - 左メニューの **Project Settings** (アイコン: ⚙️) をクリック
    - **API** をクリック
    - 以下の2つをコピーして、チャットで教えてください！
        - **Project URL**
        - **anon / public** Key

## Q&A: 名前やPINを変更したい場合

人数が増減しない限り、あとから変更可能です！
Supabaseの管理画面から、Excelのように簡単に編集できます。

1. **Table Editor** (アイコン: ﹇ ) をクリック
2. `users` テーブルを選択
3. 変えたい人の名前やPINをダブルクリックして書き換えるだけ
4. 保存ボタン等はなく、自動で反映されます
